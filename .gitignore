node_modules/
dist/
.DS_Store
*.local
.vite/

# Pipeline working files — only the built species.json is committed
pipeline/.cache/
pipeline/raw/
# ...except the source workbooks and the committed crawl output, both of which
# the build depends on.
!pipeline/raw/*.xlsx
!pipeline/raw/online_data.json
__pycache__/
*.pyc
.venv/
tsconfig.tsbuildinfo
